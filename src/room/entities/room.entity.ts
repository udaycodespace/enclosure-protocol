/**
 * Room Entity
 * Represents a room aggregate with state machine tracking.
 * States: ROOM_CREATED → INVITE_SENT → JOINED → LOCKED → IN_PROGRESS → UNDER_VALIDATION → SWAP_READY → SWAPPED (or FAILED/EXPIRED)
 */

export class Room {
  // TODO: Add entity properties and field definitions
}
